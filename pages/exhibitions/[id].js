import Slider from '../../components/Slider';
import data from '../../dataExhibitions';

const SingleExhibition = () => {
  return (
    <div>
      <Slider data={data} />
    </div>
  );
};
export default SingleExhibition;
